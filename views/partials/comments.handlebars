<section class="comments card">
  {{#each this}}
    <div class="text pinkTxt">
      {{this.user.name}} on {{format_date date_created}}:
    </div>
    <div class="commentTxt text">
      {{this.comment_text}}
    </div>
  {{/each}}
</section>